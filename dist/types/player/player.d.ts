import { Player, PlayerOptions, LavalinkManager } from "lavalink-client";
export declare class CustomPlayer extends Player {
    constructor(options: PlayerOptions, lavalinkManager: LavalinkManager);
}
